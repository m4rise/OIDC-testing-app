### Database - Postgres
PG_HOST=postgres
PG_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-database-password
POSTGRES_DB=your-database-name

### Session
SESSION_SECRET=your-super-secret-session-key-change-in-production-with-random-string

### Mock OIDC (for development)
USE_MOCK_OIDC=true

### Security Settings (for development)
# Set to true to completely disable CSP headers for easier development
DISABLE_CSP=true

# Cookie Security (Production settings - uncomment and configure for production)
# SESSION_COOKIE_NAME=app_session_prod
# COOKIE_DOMAIN=.yourdomain.com

### OIDC Configuration (update with your SSO provider details)
# Set USE_MOCK_OIDC=false and configure these for real OIDC
OIDC_CLIENT_ID=your-client-id
OIDC_CLIENT_SECRET=your-client-secret
OIDC_ISSUER=https://your-sso-provider.com
OIDC_CALLBACK_URL=https://node.localhost/api/auth/callback
OIDC_LOGOUT_URL=https://your-sso-provider.com/logout
# OIDC ACR Values - Authentication Context Class Reference
# Examples:
# - For MFA: OIDC_ACR_VALUES=mfa
# - For specific authentication strength: OIDC_ACR_VALUES=level1,level2
# - For custom authentication context: OIDC_ACR_VALUES=custom-auth-context
OIDC_ACR_VALUES=your-acr-value

# You can set multiple values separated by commas if your OIDC provider supports it
# OIDC_ACR_VALUES=mfa,level2,custom-context

### Frontend URL
FRONTEND_URL=https://front.localhost
